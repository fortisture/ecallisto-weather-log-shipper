"""DORM station server.

The pieces that run on the receiving machine: the TLS receiver that
accepts data from the Pi, the API generator that turns stored CSV and
FITS into JSON the website reads, and the HTTP server that serves it.
"""
